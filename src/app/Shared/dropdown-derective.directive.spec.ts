import { DropdownDerectiveDirective } from './dropdown-derective.directive';

describe('DropdownDerectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownDerectiveDirective();
    expect(directive).toBeTruthy();
  });
});
